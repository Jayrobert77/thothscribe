# Ownership Statement for thothscribe (Thoth-Quills-API)

Repository: https://github.com/Jayrobert77/thothscribe

I, Jayrobert77 (GitHub: https://github.com/Jayrobert77), assert that I am the creator and owner of the code and content in this repository as of the timestamps recorded in the repository history.

What this file proves:
- This file is part of the repository and therefore recorded in the Git history.
- To strengthen the proof, create a GPG-signed commit and/or a GPG-signed git tag referencing this file and the important commit SHAs.

How to independently verify my authorship:

1) Verify commits that list my Git identity (example):
   - git log --pretty=fuller --date=iso

   Look for commits where:
   - Author: `git tag -v` or `gpg --list-keys` matches the key added to GitHub.

2) Verify GPG-signed commits or tags:
   - git log --show-signature
   - git tag -v <tag-name>

3) Check the proof.txt file that should be included in a GPG-signed commit or tag.

4) External notarization (optional):
   - You may timestamp this proof using OpenTimestamps or publish the signed file to IPFS/Arweave for immutable external proof.

Signed statement template (this exact content should be signed):
---
I, Jayrobert77 (https://github.com/Jayrobert77), assert ownership of the repository https://github.com/Jayrobert77/thothscribe and the code therein. This statement includes commit SHAs and is timestamped in the Git history.

For the repository owner (Jayrobert77):
To create a GPG-signed proof, follow the instructions in CONTRIBUTING.md to create a signed commit or signed tag that includes the proof.txt file. This provides cryptographic proof of ownership that can be independently verified.